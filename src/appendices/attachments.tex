\chapter*{Mellékletek}
\vspace{2 cm}

\begin{figure}[ht]
    \hspace*{-1cm}
    \centering
    \begin{minted}{BNF}
<constraint>               ::= <suite> "&" <build-option-constraints>

<build-option-constraints> ::= <build-option-constraints> "&" <build-option>
                             | <build_option>

<build-option>             ::= <id>
                             | <id> ":" <id>

<suite>                    ::= "*"
                             | <id>

<id>                       ::= non empty string
    \end{minted}
    \caption{\label{fig:bnf} A \textit{Constraints} konfigurációk nyelvtana \textbf{Backus-Naur-formában}}
\end{figure}

\begin{figure}[ht]
    \centering
    \begin{minted}{python}
class ConfigManager:
    def __init__(self, config_file: "StrOrPath", section: str = "DEFAULT") -> None: ...
    def get_item(self, item: str, default: str | None = None) -> str: ...
    def get_item_default(self, item: str, default: str | None = None) -> str: ...
    def get_bool(self, item: str, *, default: bool | None = None) -> bool: ...
    def get_bool_default(self, item: str, *, default: bool | None = None) -> bool: ...
    def get_int(self, item: str, default: int | None = None) -> int: ...
    def get_int_default(self, item: str, default: int | None = None) -> int: ...
    def get_float(self, item: str, default: float | None = None) -> float: ...
    def get_float_default(self, item: str, default: float | None = None) -> float: ...
    def get_lines(self, item: str, default: str | None = None) -> list[str]: ...
    def get_lines_default(self, item: str, default: str | None = None) -> list[str]: ...
    def get_list(self, item: str, default: str | None = None, sep: str | None = None) -> list[str]: ...
    def get_list_default(self, item: str, default: str | None = None, sep: str | None = None) -> list[str]: ...
    \end{minted}
    \caption{\label{fig:config-manager} A \textit{ConfigManager} publikus interfésze}
\end{figure}

\begin{figure}[ht]
    \centering
    \begin{minted}{INI}
[DEFAULT]
TRIPLE = aarch64-none-none-elf
ARCH = ARMV8A
RCB = COMPILE_LINK_RUN

SUITES =
    LOOPS
    C99LANG
    C99LIB
    Cxx17LANG
    Cxx17LIB

EXCEPTIONS =
    EX
    NEX

BUILTIN =
    BI
    NBI

MATRIX =
    ARCH
    EXCEPTIONS
    BUILTIN

SKIPLISTS =
    *&@skiplist_libcxx_atomic
    *&@skiplist_libcxx_chrono

CONSTRAINTS =
    LOOPS&BUILTIN&EXCEPTIONS
    C99LANG&EXCEPTIONS
    C99LIB&EXCEPTIONS

TIMEOUT_COMPILE = *&240

CFLAGS =
    *&-Wall
    *&-ffunction-sections
    *&-fdata-sections
    *&-fsized-deallocation
    *&-fno-fast-math
    \end{minted}
    \caption{\label{fig:config-full} Példa egy konfigurációs fájlra}
\end{figure}
